// package.json (root)
{
  "name": "fullstack-auth-app",
  "version": "1.0.0",
  "scripts": {
    "start-server": "nodemon server.js",
    "start-client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run start-server\" \"npm run start-client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  }
}

// 📂 Folder structure (common in company projects):
// root/
//   server.js (backend)
//   client/ (React + Tailwind frontend)
//   package.json (root, with scripts above)

// 🚀 Usage:
// 1. In the root folder: npm install
// 2. In client folder: npm install
// 3. In root: npm run dev
// This runs backend (server.js) and frontend (React dev server) together.
// This concurrent dev workflow is widely used in companies for local development.

// ℹ️ Notes:
// - Backend runs on port 4000, frontend on 3000.
// - React code uses full API URLs (http://localhost:4000/...).
// - CORS is already enabled in backend.
// - For production, build React and serve static files from backend.